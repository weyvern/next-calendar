import React from 'react'

const Home = () => (
  <div>
    You can go to /calendar/:mentorId. Currently only id 1 and 2 have events
    loaded. You can list the events by going to /api/events; or by mentorId, to
    /api/events/:mentorId
  </div>
)
export default Home
